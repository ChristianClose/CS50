#include <cs50.h>
#include <stdio.h>
#include <string.h>

int countLetters(string text);

int main(void)
{
    string text = get_string("Text: \n");


}

int countLetters(string text)
{
    for(int i =0; i < strlen(text); i++){
        if()
    }
}